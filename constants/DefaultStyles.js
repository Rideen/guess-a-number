import { StyleSheet } from 'react-native';

// alternative to small component wrappers (TitleText / BodyText) - not used
// can be used instead of component driven approach for styling
export default StyleSheet.create({
    bodyText: {
        fontFamily: 'open-sans'
    }
});